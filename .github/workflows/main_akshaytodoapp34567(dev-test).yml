name: Build and Deploy Flask App

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:

    runs-on: ubuntu-latest

    steps:

    - name: Checkout code
      uses: actions/checkout@v4

    - name: Login to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Build Docker image
      run: |
        docker build -t ${{ secrets.DOCKER_USERNAME }}/flask-todo-app:v4 .

    - name: Push image
      run: |
        docker push ${{ secrets.DOCKER_USERNAME }}/flask-todo-app:v4

    - name: Deploy Web App
      uses: azure/webapps-deploy@v3
      with:
        app-name: akshaytodoapp34567
        slot-name: dev-test
        publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
        images: ${{ secrets.DOCKER_USERNAME }}/flask-todo-app:v4
