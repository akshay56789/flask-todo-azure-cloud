name: Build and Deploy Flask App

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:

    runs-on: ubuntu-latest

    steps:

    - name: Checkout code
      uses: actions/checkout@v4

    - name: Login to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Build Docker image
      run: |
        docker build -t ${{ secrets.DOCKER_USERNAME }}/flask-todo:azure .

    - name: Push image
      run: |
        docker push ${{ secrets.DOCKER_USERNAME }}/flask-todo:azure

    - name: Azure Login
      uses: azure/login@v2
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}

    - name: Deploy Web App
      uses: azure/webapps-deploy@v3
      with:
        app-name: YOUR_WEBAPP_NAME
        images: ${{ secrets.DOCKER_USERNAME }}/flask-todo:azure
